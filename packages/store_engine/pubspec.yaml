name: store_engine
description: App store specifications and asset layout generator engine for Google Play, App Store, Huawei, Amazon, and Microsoft Store.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  image: ^4.2.0
  image_engine:
    path: ../image_engine
  meta: ^1.11.0
  path: ^1.9.0
  shared:
    path: ../shared

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
