name: config_engine
description: Configuration parsing and validation engine for flutter_marketing_kit.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shared:
    path: ../shared
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
