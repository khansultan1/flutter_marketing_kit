name: screenshot_engine
description: Production-grade screenshot capture and automation engine for flutter_marketing_kit.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  image: ^4.2.0
  meta: ^1.11.0
  navigation_engine:
    path: ../navigation_engine
  path: ^1.9.0
  shared:
    path: ../shared

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
